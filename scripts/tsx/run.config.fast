do_compile=0;
set_cpu=0;
repetitions=3;
duration=1000;
keep=median; #max min median
cores=ppopp;

ub="./bin/$(uname -n)";
uo="scripts/tsx/data";
fixed_file_dat=1;		#so that the gnuplot scripts are not parametrized by
				#the platform name 				

